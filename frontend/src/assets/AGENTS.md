# Bundled Asset Guidelines

- Store assets here only when they must be imported and processed by Vite.
- Use descriptive lowercase filenames, optimize image dimensions, and prefer SVG for simple scalable graphics.
- Provide meaningful alt text at the usage site; decorative images must use empty alt text.
- Remove replaced assets so the production bundle does not retain dead files.
